<div class="dice-roll">
  {{#if flavor}}
  <div class="dice-flavor">
    {{{flavor}}}
    {{{additionalFlavor}}}
  </div>
  {{/if}}
  <div class="dice-result">
    <div class="dice-formula">{{formula}}</div>
    {{{tooltip}}}
    <h4 class="dice-total {{totalClass}}">{{total}}</h4>
  </div>
</div>